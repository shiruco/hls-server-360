name: Deploy to app
on:
  push:
    branches:
      - develop
    paths:
      - client/src/**
    tags:
      - v*

defaults:
  run:
    shell: bash

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: test.
        run: |
          echo "Hello"
